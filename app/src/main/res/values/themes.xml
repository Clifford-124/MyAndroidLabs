<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CliffordLabs" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/myprimary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/mysecodary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/mystatusbar </item>
        <!-- text color. -->
        <item name="android:textColor">@color/mytextcolor</item>

        <!-- Customize your theme here. -->

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>